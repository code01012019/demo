---
  - hosts: TEST
    tasks:
      - name: Install httpd package
        yum:
          name: httpd
          state: present
          debug:
            msg: 'Apache httpd is installed'
      - name: Check httpd service is running
        service:
          name: httpd
          state: started
          debug:
            msg: 'Apache httpd is started'